# que es ETL
Extraccion transformacion load(subir datos), el flujo que se hace con los datos
# Que hace SQL dentro del flujo data
SQL es la parte consultas para la extraccion
# que hace Python
Python se encarga de la automatizacion
# que hace power bi
Se encarga de la visualizacion
# que hace realmente un analista
conjuga las tres herramientas para poder sacar el maximo probecho