# que hace el if
el if es una condicional que verifica un enunciado booleano
# que hace el else
es el complemento del if si la validacion es incorrecta el flujo va al else
# que hace el elif
es un intermedio entre el else y el if, para una tercera condicion
# diferencia entre = y == en python
un = es asignacion, y == es verificacion
# porque las condicionales son importantes
porque podemos tener un flujo mas compacto para tomar decisiones